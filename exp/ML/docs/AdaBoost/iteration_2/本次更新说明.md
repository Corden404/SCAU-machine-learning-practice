# 本次更新说明

## 迭代定位

本轮是 AdaBoost 的第 2 次迭代，根据数据预分析结论进行针对性加强。

## 相比上次的变化

1. 将 `learning_rate` 调整为 `0.5`，降低每一轮弱分类器对最终模型的影响。
2. 该调整用于观察 AdaBoost 是否会因为离群点或难分样本被反复加权而产生不稳定表现。
3. 增加 5 折交叉验证和多随机种子分层划分，用于观察模型在小样本数据上的稳定性。

