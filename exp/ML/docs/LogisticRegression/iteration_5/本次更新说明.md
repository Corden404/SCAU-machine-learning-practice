# 本次更新说明

## 迭代定位

iteration_5 围绕 iteration_4 做收敛与复杂度控制，不追求更高指标，而是验证迭代 4 结论的稳健性。

## 相比上次的变化

1. **实验一**：对比 `interaction_only=True` 和完整二阶多项式 → 平方项是必要的，CV macro F1 差距 0.04。
2. **实验二**：L2 C 精细搜索（0.1 ~ 10.0）→ C >= 2.0 的平台期内任意值等价，C=3.0 不唯一。
3. **实验三**：L1 / ElasticNet 稀疏对比 → ElasticNet (C=0.3, l1_ratio=0.8) 仅用 100/594 特征达到 0.9360，接近完整模型。
4. **实验四**：输出完整二阶多项式的系数排名 → 平方项和 f1 交叉项占主导。
5. **实验五**：interaction_only 最佳配置的完整评估，与 iter_4 对比。

## 核心结论

- iteration_4 的 L2 + C=3.0 配置是稳健且合理的。
- 如需要，可补充 ElasticNet 稀疏方案（100 特征）用于可解释性分析。
- 逻辑回归迭代到此可以收敛，后续应转向其他模型。
