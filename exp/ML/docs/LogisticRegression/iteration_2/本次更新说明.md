# 本次更新说明

## 迭代定位

本轮是 LogisticRegression 的第 2 次迭代，根据数据预分析结论进行针对性加强。

## 相比上次的变化

1. 在模型前加入 `VarianceThreshold`，用于去除常量特征，例如预分析中发现的 `f2`。
2. 在模型前加入 `StandardScaler`，减少特征尺度对线性模型训练和系数稳定性的影响。
3. 将逻辑回归最大迭代次数提高到 `1000`，降低小样本训练中收敛不足的风险。
4. 增加 5 折交叉验证和多随机种子分层划分，用于观察小样本数据下结果是否稳定。

