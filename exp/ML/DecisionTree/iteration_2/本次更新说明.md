# 本次更新说明

## 迭代定位

本轮是 DecisionTree 的第 2 次迭代，根据数据预分析结论进行针对性加强。

## 相比上次的变化

1. 设置 `max_depth=4`，限制树深度，降低小样本数据中过拟合训练细节的风险。
2. 设置 `min_samples_leaf=5`，避免叶节点样本过少导致模型过度拟合局部样本。
3. 增加 5 折交叉验证和多随机种子分层划分，用于观察单棵树在小样本下的稳定性。

