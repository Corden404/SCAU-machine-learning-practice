# 本次更新说明

## 迭代定位

本轮是 DecisionTree 的第 1 次迭代，用于建立默认参数 baseline。

## 相比上次的变化

1. 这是该模型的初始版本，没有上一轮可对比。
2. 使用默认参数决策树，在原始 Ionosphere 特征上训练和测试。
3. 按统一评价流程输出 accuracy、precision、recall、F1、ROC-AUC、混淆矩阵和 ROC 曲线。

